# Example for jig-yaml
name: {{ projectName }}
version: {{ version }}
enabled: @if(isEnabled) true @else false @end
settings:
  theme: {{ theme }}
  features:
    @each(feature in features)
      - {{ feature }}
    @end