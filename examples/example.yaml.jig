# Example for jig-yaml
name: {{ projectName }}
version: {{ version }}
enabled: @if(isEnabled) true @else false @endif
settings:
  theme: {{ theme }}
  features:
    @each(feature in features)
      - {{ feature }}
    @endeach