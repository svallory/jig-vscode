{
  "name": "{{ projectName }}",
  "version": "{{ version }}",
  "enabled": @if(isEnabled) true @else false @end,
  "settings": {
    "theme": "{{ theme }}",
    "features": [
      @each(feature in features)
        "{{ feature }}"@if(!loop.last),@end
      @end
    ]
  }
}