// Example for jig-fsharp
function init() {
  const config = {
    apiKey: '{{ secrets.apiKey }}',
    environment: '{{ env }}'
  };

  @if(debugMode)
    console.log('Jig is active for jig-fsharp');
  @endif

  const list = [
    @each(val in values)
      '{{ val }}',
    @endeach
  ];

  return config.{{ property }};
}

{{-- Jig structures interleaved with host language --}}