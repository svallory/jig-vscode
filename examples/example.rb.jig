def main
  # Example for jig-ruby
  title = "{{ title }}"
  puts "Title: #{title}"

  @if(debugMode)
    puts "Debug mode is active"
  @end

  items = [
    @each(item in items)
      "{{ item }}",
    @end
  ]

  {{-- Jig comment in Ruby --}}
  p items
end