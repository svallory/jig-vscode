<?php
// Example for jig-php
$title = "{{ title }}";

@if(debugMode)
  echo "Debug mode is active";
@end

$items = [
  @each(item in items)
    "{{ item }}",
  @end
];

{{-- Jig comment in PHP --}}
foreach ($items as $item) {
  echo $item;
}