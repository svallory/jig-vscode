// Example for jig-r
function init() {
  const config = {
    apiKey: '{{ secrets.apiKey }}',
    environment: '{{ env }}'
  };

  @if(debugMode)
    console.log('Jig is active for jig-r');
  @endif

  const list = [
    @each(val in values)
      '{{ val }}',
    @endeach
  ];

  return config.{{ property }};
}

{{-- Jig structures interleaved with host language --}}