// Example for jig-vb
function init() {
  const config = {
    apiKey: '{{ secrets.apiKey }}',
    environment: '{{ env }}'
  };

  @if(debugMode)
    console.log('Jig is active for jig-vb');
  @end

  const list = [
    @each(val in values)
      '{{ val }}',
    @end
  ];

  return config.{{ property }};
}

{{-- Jig structures interleaved with host language --}}