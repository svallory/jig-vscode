def main():
    # Example for jig-python
    title = "{{ title }}"
    print(f"Title: {title}")

    @if(debugMode)
    print("Debug mode is active")
    @endif

    items = [
        @each(item in items)
        "{{ item }}",
        @endeach
    ]

    {{-- Jig comment in Python --}}
    return items